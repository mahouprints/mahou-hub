{
  "mcpServers": {
    "mahou-oportunidades": {
      "command": "node",
      "args": ["./mcp-servers/mahou-oportunidades/dist/index.js"],
      "env": {
        "MAHOU_API_URL": "${MAHOU_API_URL:-https://api.mahouprints.com}",
        "MAHOU_API_TOKEN": "${MAHOU_API_TOKEN}"
      }
    }
  }
}
